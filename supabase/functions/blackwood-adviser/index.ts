import { createClient } from "npm:@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"};
const GAME_GUIDE=`You are The Consigliere, the in-world guide for Blackwood City, an online mafia browser RPG packaged for Android. Be concise, practical and honest. Only recommend pages present in available_pages. The authoritative systems are: crimes use nerve; gym uses energy; combat targets real protected players; professions require a three-question interview and 20-hour shifts; inventory has eight equipment slots and consumables; the Economy has six simulated 24/7 Forex pairs with spread, margin and leverage; Federal Trust banking careers require a proven Forex record; Rossi's casino offers blackjack, European roulette and slots; family, world chat, mail, forums and rankings are online. Never claim you performed an action. Never invent account values or players. Warn that leverage can lose the full margin. Use the supplied account context to identify what is currently possible and give at most three useful next moves.`;
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});try{const auth=req.headers.get("Authorization");if(!auth)throw new Error("Sign in required");const url=Deno.env.get("SUPABASE_URL")!,anon=Deno.env.get("SUPABASE_ANON_KEY")!;const db=createClient(url,anon,{global:{headers:{Authorization:auth}}});const{data:context,error}=await db.rpc("bw_adviser_context");if(error)throw error;const body=await req.json().catch(()=>({}));const question=String(body.question||"What should I do next?").slice(0,800);const key=Deno.env.get("OPENAI_API_KEY");if(!key)throw new Error("The adviser is awaiting its OPENAI_API_KEY secret");const model=Deno.env.get("OPENAI_MODEL")||"gpt-5.6";const response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({model,store:false,input:[{role:"developer",content:GAME_GUIDE},{role:"user",content:`ACCOUNT CONTEXT\n${JSON.stringify(context)}\n\nPLAYER QUESTION\n${question}`}],text:{format:{type:"json_schema",name:"blackwood_advice",strict:true,schema:{type:"object",additionalProperties:false,properties:{answer:{type:"string"},suggestions:{type:"array",maxItems:3,items:{type:"object",additionalProperties:false,properties:{label:{type:"string"},page:{type:"string",enum:context.available_pages},reason:{type:"string"}},required:["label","page","reason"]}}},required:["answer","suggestions"]}}})});if(!response.ok)throw new Error(`OpenAI request failed (${response.status})`);const result=await response.json();return new Response(result.output_text,{headers:{...cors,"Content-Type":"application/json"}})}catch(error){return new Response(JSON.stringify({error:error.message}),{status:400,headers:{...cors,"Content-Type":"application/json"}})}});
